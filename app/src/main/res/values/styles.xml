<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="buttonStyle">@style/ButtonOrange</item>
        <item name="dialogTheme">@style/dialogTheme</item>
        <item name="alertDialogTheme">@style/dialogTheme</item>
    </style>

    <style name="dialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.TextAppearance">
        <!-- <item name="fontPath">fonts/FiraSans-Regular.otf</item> -->
    </style>

    <style name="AppTheme.TextAppearance.Bold">
        <!-- <item name="fontPath">fonts/FiraSans-Bold.otf</item> -->
        <item name="android:textColor">#4A4A4A</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.TextAppearance.Medium">
        <!-- <item name="fontPath">fonts/FiraSans-Medium.otf</item> -->
        <item name="android:textColor">#4A4A4A</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="light_toolbar" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="white_toolbar" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@android:color/black</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <style name="Toolbar.TitleText" parent="AppTheme.TextAppearance.Bold">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="ButtonOrange" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:textColor">@android:color/white</item>
        <!-- <item name="android:background">@drawable/selectable_orange</item> -->
        <item name="android:foreground">?selectableItemBackground</item>
    </style>

    <style name="AppTheme.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
